import { FC } from 'react'

const NotFoundPage: FC = () => {
	return (
		<div>
			The tracking time seems to have expired or the link is invalid,
			please contact the link owner
		</div>
	)
}

export default NotFoundPage
